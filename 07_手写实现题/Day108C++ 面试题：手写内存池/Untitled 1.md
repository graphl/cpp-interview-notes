## 3. 原理

初始化时一次申请大块内存，然后切成多个固定大小的块。

释放的内存块不会还给系统，而是挂回空闲链表。

下次分配时直接从空闲链表取一个块，时间复杂度 O(1)。

---

## 4. 注意点

真实内存池还需要考虑：

1. 内存对齐
2. 多线程安全
3. 不同大小块管理
4. 扩容策略
5. 析构对象和释放原始内存的区别

---

## 5. 面试回答

内存池的核心是一次申请一大块内存，切分成固定大小的小块，并用空闲链表管理。分配时从空闲链表取一个块，释放时把块挂回链表。这样可以减少频繁申请释放内存的开销，并降低内存碎片。