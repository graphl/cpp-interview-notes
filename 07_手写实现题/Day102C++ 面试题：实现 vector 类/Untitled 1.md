## 真实 vector 更复杂在哪里？

真实 `std::vector` 会区分：

1. 已分配但未构造的内存
2. 元素构造和析构
3. allocator
4. 异常安全
5. 移动构造优化

---

##  面试回答

`vector` 本质是连续内存动态数组。插入元素时，如果容量不足，就重新申请更大的空间，通常按 2 倍扩容，然后把旧元素搬过去并释放旧空间。因为扩容会改变底层地址，所以可能导致迭代器、指针和引用失效。