##  特殊情况：`void*` 指针

虽然 `void` 本身没有大小，但是 **`void\*` 是一种正常的指针类型**，它能指向任意类型的数据：

```
int* p1;
double* p2;
void* pv;
```

- `sizeof(void*)` 是有定义的，通常等于机器字长：
  - 32 位系统：4 字节
  - 64 位系统：8 字节
- `sizeof(void*)` 不一定等于 `sizeof(int*)`，但在大部分平台上是相等的（C++ 标准并不保证一定相等）。