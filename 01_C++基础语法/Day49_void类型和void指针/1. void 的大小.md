##  `void` 的大小

- **标准规定**：
   C++ 标准里没有定义 `sizeof(void)` 的结果，因为 `void` 不是完整类型（incomplete type）。

- 在大多数编译器上，**不能写**：

  ```
  std::cout << sizeof(void); // ❌ 编译错误
  ```

  因为 `sizeof` 需要一个完整类型。

所以，严格来说：
 👉 **`void` 没有大小**。



