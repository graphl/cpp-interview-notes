

# ⚠️ 注意事项

| 点         | 说明                                       |
| ---------- | ------------------------------------------ |
| ❌ 不是继承 | `friend` 不是继承关系，不是 is-a 关系。    |
| ✅ 单向授权 | A 声明 B 为友元 → B 可访问 A；反之不行。   |
| ✅ 不是成员 | 友元函数**不属于类成员**，只是被授权访问。 |
| ✅ 可以重载 | 友元函数可以重载，如重载 `operator<<`。    |