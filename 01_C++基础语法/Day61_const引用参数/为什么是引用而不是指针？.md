## 为什么是引用而不是指针？

- C++ 的面向对象设计希望拷贝构造函数形式自然，和普通函数调用保持一致。
- 如果用指针 `A(A* other)`：
  - 语义上不像“拷贝”，更像“通过指针初始化”。
  - 用户必须写 `A a2(&a1);`，这不直观。
  - 不能保证调用时传入的指针非空。

所以选择 **引用**，既符合直觉（就是“拷贝另一个对象”），又能避免指针带来的麻烦。

