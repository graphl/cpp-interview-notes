## 如果参数不是引用，会发生什么？

假设我们写成：

```
A(A other);   // 错误示例：按值传递
```

当你调用拷贝构造函数时，比如：

```
A a1;
A a2(a1);  // 调用拷贝构造函数
```

传参过程是 **按值传递**，所以 `a1` 会先被复制一份作为参数 `other`。
 但复制 `a1` 的时候，又需要调用拷贝构造函数！

于是：

- 调用拷贝构造函数时，需要拷贝参数
- 拷贝参数时，又需要调用拷贝构造函数
- 无限递归下去，编译器无法生成代码

👉 这就是为什么拷贝构造函数不能按值传递，只能用 **引用**。

