## 三、注意事项（陷阱 ⚠️）

1. **不要返回局部变量的引用**

```
int& foo() {
    int x = 10;
    return x;   // ❌ 错误：x 在函数结束时销毁
}
```

1. **常量引用避免拷贝**
    对于大对象输入参数，用 `const &` 传递最合适：

```
void print(const std::vector<int>& v);
```

1. **右值引用**
    C++11 之后还可以用 `T&&`（右值引用），避免临时对象拷贝并支持移动语义。