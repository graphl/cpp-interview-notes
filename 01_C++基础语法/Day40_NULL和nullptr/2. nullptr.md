## 2. **nullptr**

- C++11 引入的关键字，专门表示 **空指针常量**。

- 类型是 `std::nullptr_t`，可以隐式转换为任何指针类型，但不能转换为整数：

  ```
  void f(int);
  void f(char*);
  
  f(nullptr);   // 调用 f(char*)，不会二义性
  f(0);         // 调用 f(int)
  ```

- 解决了 `NULL` 带来的二义性问题。