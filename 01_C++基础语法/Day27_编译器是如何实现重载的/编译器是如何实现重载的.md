# 编译器是如何实现重载的

编译器实现函数**重载（overload）\**的核心机制是\**名称修饰（Name Mangling）** + **符号表匹配**。我给你分步骤解释，这个是面试高频问题。

## 1. **基本原理**

C++ 支持函数重载，即同一个函数名可以有不同参数列表：

```
void foo(int);
void foo(double);
```

为了区分它们，编译器会在**编译阶段**把 `foo` 转换成**唯一的符号名**（mangled name），这个过程叫 **Name Mangling**。

