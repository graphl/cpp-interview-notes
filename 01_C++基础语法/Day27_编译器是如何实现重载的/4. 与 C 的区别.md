## 4. **与 C 的区别**

C 语言**不支持重载**，因为 C 编译器不会做 name mangling：

```
// C
void foo(int);
void foo(double); // ❌ 重定义错误
```

C 编译器符号名就是 `foo`，所以会冲突。
 在 C++ 中，如果你想让函数用 C 方式命名（无 name mangling），可以用：

```
cpp


复制编辑
extern "C" void foo(int);
```

这样就无法重载了。