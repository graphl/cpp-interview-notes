##  核心区分

- **虚函数表（vtable）**：存放虚函数地址的表，**每个类只有一份自己的 vtable**（即 Derived 只有一张 Derived 的 vtable）。
- **虚表指针（vptr）**：对象内部指向虚表的指针。
  - **多继承时，每个基类子对象都可能有自己的 vptr**，因为每个基类部分可能有独立的虚函数布局。

所以即使 `Derived` 只有 **一张虚表**，对象内也可能有 **多个 vptr**，分别对应基类部分。