##  为什么放在只读数据区（.rodata）

### (1) 安全性

- 如果放在可写区域（.data 或 .bss），恶意代码可以修改 vtable，导致函数调用被劫持（比如把函数指针改成恶意地址 → ROP 攻击）。
- 放在只读区，可以避免程序运行时篡改。

### (2) 不需要修改

- vtable 的内容（即函数指针）在编译/链接时就决定了，不会在运行时改变。
- 所以它完全可以放在 **只读常量区**，避免浪费可写内存。

### (3) 高效共享

- 不同对象共享同一个 vtable。
- 把它放在只读全局区，可以保证所有对象实例访问到的是同一张表，节省内存。