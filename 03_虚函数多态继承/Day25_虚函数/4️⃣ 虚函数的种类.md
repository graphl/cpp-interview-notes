# 4️⃣ 虚函数的种类

- **普通虚函数**
   通过 `virtual` 声明，可被子类重写

- **纯虚函数（抽象函数）**

  ```
  cpp
  
  
  复制编辑
  virtual void foo() = 0; // 无实现
  ```

  类中有纯虚函数 → 该类是抽象类，不能实例化

- **虚析构函数**
   基类有虚析构函数，可以正确释放派生类对象

- **虚拷贝构造（不推荐）**
   语法支持，但意义不大