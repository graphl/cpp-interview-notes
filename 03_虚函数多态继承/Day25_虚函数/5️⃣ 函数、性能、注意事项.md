## 5️⃣ 函数、性能、注意事项

- 动态绑定需要一次 **额外的指针间接跳转**
- 虚函数表查找开销非常小（一般一个 CPU 指令左右）
- 如果在性能极端敏感的地方，可用**静态多态（模板/内联）**替代
- **构造函数中调用虚函数 → 调用的是当前类版本**
- **析构函数建议声明为 `virtual`**，否则通过基类指针 `delete` 派生类对象会导致析构不完全
- 重写时函数签名必须完全匹配（建议加 `override` 关键字）
- 虚函数不能是 `static`
- 构造函数不能是虚函数（但析构函数可以）