## 📌 总结流程图

```
操作系统加载程序
        ↓
运行时入口 _start
        ↓
初始化运行时 (堆, I/O, TLS)
        ↓
全局/静态对象构造
        ↓
调用 main(argc, argv, envp)
        ↓
main 返回
        ↓
全局/静态对象析构
        ↓
资源清理 & exit()
        ↓
进程退出
```