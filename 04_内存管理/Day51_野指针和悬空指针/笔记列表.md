## 悬空指针（Dangling Pointer）

- **定义**：指针原本指向一块有效的内存，但这块内存已经被释放或失效，指针却还保存着原地址。

- **特点**：曾经合法 → 现在非法。

- **典型场景**：

  - `delete` 或 `free` 之后未置空。
  - 返回局部变量的地址。
  - 对象析构后继续访问其成员。

- **示例**：

  ```
  int* p = new int(10);
  delete p;   // 内存释放
  *p = 5;     // 悬空指针，未定义行为
  ```