## 野指针（Wild Pointer）

- **定义**：指针没有被初始化，或者被赋予一个随机的、不可控的地址。

- **特点**：从来就没合法过，指向未知内存。

- **典型场景**：

  - 指针未初始化就使用。
  - 指针越界，指向了未分配区域。
  - 随意强制类型转换得到的地址。

- **示例**：

  ```
  int* p;     // 未初始化，值是随机的
  *p = 5;     // 野指针，未定义行为
  ```

