## 方法 2：继承“禁止堆分配”基类

```
class NoHeap {
private:
    void* operator new(size_t) = delete;
    void operator delete(void*) = delete;
};

class StackOnly : public NoHeap {
public:
    StackOnly() = default;
};

int main() {
    StackOnly s;  // ✅
    // auto p = new StackOnly(); // ❌
}

```