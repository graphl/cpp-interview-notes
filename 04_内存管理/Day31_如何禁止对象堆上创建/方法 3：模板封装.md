## 方法 3：模板封装

如果有很多类都要限制：

```
template <typename T>
class StackOnlyBase {
private:
    void* operator new(size_t) = delete;
    void operator delete(void*) = delete;
};

class MyClass : public StackOnlyBase<MyClass> {
public:
    MyClass() {}
};
```

