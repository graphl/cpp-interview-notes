## 🧠 一、为什么会有「栈溢出」？

### 1️⃣ 栈的本质

在每个进程（线程）启动时，系统都会给它分配一块**栈空间（Stack Segment）**，用于：

- 存放函数的**局部变量**
- 保存**函数调用的返回地址**
- 存储**函数参数**
- 保存**寄存器上下文**

也就是说，**每一次函数调用，都会在栈上分配一小块空间（栈帧 / Stack Frame）**。
 函数结束后这块空间被释放，栈顶指针退回。