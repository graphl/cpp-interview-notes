# 扩容策略

不同 STL 实现可能略有不同，但一般遵循以下规则：

**常见倍增策略**：

- 扩容大小通常是 **原容量的 2 倍**（或 1.5 倍）。

- 举例：

  ```
  capacity = 4 → push_back 第 5 个元素 → capacity 扩容为 8
  capacity = 8 → push_back 第 9 个元素 → capacity 扩容为 16
  ```

**最小扩容保证**：

- 即便 vector 原容量为 0，也会给它分配至少 1 个元素空间。