# ⚡ 四、array – 最稳定

| 操作         | 迭代器是否失效                   |
| ------------ | -------------------------------- |
| 所有元素访问 | 不失效（固定大小）               |
| 赋值 `=`     | 左容器迭代器失效（重新复制元素） |

📌 口诀：

> array 除了整个赋值以外都稳。

