# C++17 专题索引

| 序号 | 专题 | 核心问题 |
|---:|---|---|
| 00 | C++17 总览 | C++17 的定位、完整特性清单和版本辨析 |
| 01 | 结构化绑定 | 将元组状对象拆分为多个名字 |
| 02 | `if/switch` 初始化语句 | 缩小查询结果等临时变量的作用域 |
| 03 | `if constexpr` | 在编译期选择模板分支 |
| 04 | 内联变量 | 在头文件中安全定义共享变量 |
| 05 | 折叠表达式 | 使用运算符展开模板参数包 |
| 06 | 类模板参数推导 | 从构造实参推导类模板参数 |
| 07 | 保证的复制消除 | 纯右值如何直接构造目标对象 |
| 08 | 标准属性 | `nodiscard`、`maybe_unused`、`fallthrough` |
| 09 | `optional` | 表达一个可能不存在的值 |
| 10 | `variant` | 表达封闭的多类型集合 |
| 11 | `any` | 使用类型擦除保存开放类型 |
| 12 | `string_view` | 非拥有字符串视图与生命周期 |
| 13 | `filesystem` | 标准路径和文件系统操作 |
| 14 | `apply` 和 `invoke` | 元组展开与统一调用接口 |
| 15 | 并发锁增强 | `shared_mutex` 和 `scoped_lock` |
| 16 | `charconv` | 低开销数值与字符转换 |
| 17 | PMR 内存资源 | 将容器与内存资源解耦 |
| 18 | 并行算法 | 执行策略及并发约束 |
| 19 | 关联容器增强 | 节点句柄、`try_emplace`、`insert_or_assign` |

推荐顺序：

```text
结构化绑定 -> if 初始化语句 -> if constexpr
-> 内联变量 -> 折叠表达式 -> CTAD -> 复制消除
-> optional -> variant -> any -> string_view
-> filesystem -> 调用工具 -> 并发与性能工具
```
