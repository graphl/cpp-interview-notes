# C++14：数字分隔符

C++14 允许在数字字面量中使用单引号分隔数字，提高可读性：

```cpp
int population = 1'000'000;
unsigned mask = 0b1111'0000;
double value = 3.141'592'6;
```

分隔符不改变数值和类型，也不需要按照固定的三位或四位分组。它不能出现在字面量开头、末尾或小数点相邻的非法位置。
