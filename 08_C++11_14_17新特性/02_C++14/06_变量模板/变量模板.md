# C++14：变量模板

变量模板允许为不同类型生成一组变量或编译期常量：

```cpp
template <typename T>
constexpr T pi = T(3.1415926535897932385L);

double d = pi<double>;
float f = pi<float>;
```

它减少了只为保存常量而定义类模板静态成员的样板代码。C++17 标准库中的 `_v` 类型特征快捷形式，例如 `std::is_same_v<T, U>`，就是变量模板的典型应用。
