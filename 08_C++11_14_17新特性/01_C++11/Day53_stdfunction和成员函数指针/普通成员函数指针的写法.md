##  普通成员函数指针的写法

成员函数指针的语法很特别，例如：

```
struct Foo {
    void show(int x) { std::cout << "Foo::show " << x << std::endl; }
};

int main() {
    Foo obj;
    void (Foo::*fp)(int) = &Foo::show; // 成员函数指针
    (obj.*fp)(42); // 调用
}
```

区别：

- 普通函数指针：`void (*)(int)`
- 成员函数指针：`void (Class::*)(int)`

