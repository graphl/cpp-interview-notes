## 什么是 `std::function`？

- `std::function` 是 C++11 引入的 **通用可调用对象包装器**。
- 它可以存储和调用任意符合指定函数签名的“可调用对象”，包括：
  - 普通函数
  - 函数指针
  - lambda（有无捕获都行）
  - 仿函数（重载了 `operator()` 的对象）
  - 绑定后的成员函数（通过 `std::bind`）

## 基本语法

```
std::function<返回类型(参数类型列表)> 变量名;
```