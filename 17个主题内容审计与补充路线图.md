# C++ 笔记：17 个主题内容审计与补充路线图

## 1. 审计目标

这套笔记不只要“题目多”，还要让第一次打开目录的人能够完成下面四件事：

1. 知道为什么要学这个主题，以及它依赖哪些前置知识。
2. 沿着推荐顺序，从概念、数据结构一路走到执行流程和工程边界。
3. 每个独立问题都能找到最小示例、常见错误和面试口述版。
4. 遇到难以通过静态文字理解的内容时，能借助流程图或短视频建立动态模型。

## 2. 统一验收标准

每个大主题至少应具备一个入口 `README.md`，并回答：

1. 这个主题解决什么问题？
2. 推荐学习顺序是什么？
3. 哪些题目是必会、进阶和岗位专项？
4. 哪些概念容易混淆？
5. 学完后应该能画出什么数据流、控制流或内存布局？

每篇核心笔记尽量包含：问题背景、最小示例、底层本质、边界与风险、复杂度或成本、面试口述版。不是所有笔记都需要机械套用六段式，但不能只保留结论。

## 3. 17 个主题审计结论

| 主题 | 当前状态 | 主要优点 | 优先补充内容 |
|---|---|---|---|
| 01 基础语法 | 中等，内容较碎 | 关键字、指针引用和运算规则较多 | 四种类型转换、未定义行为、整数提升、对象生命周期基础；建立导航 |
| 02 类和对象模型 | 中上，有重复 | 构造析构、拷贝控制、对象布局较全 | EBO、standard-layout/trivial/POD、运算符重载边界；合并重复学习路径 |
| 03 虚函数多态继承 | 较完整 | vtable、虚继承、多继承覆盖深入 | RTTI、`dynamic_cast`、`typeid`、去虚拟化；增加对象布局动态图 |
| 04 内存管理 | 中上 | 泄漏、悬空、对齐、placement new 已覆盖 | 简化 malloc/free、operator new/delete、分配器、内存块合并与碎片 |
| 05 智能指针和 RAII | 中等 | 三类智能指针和控制块主线清楚 | `enable_shared_from_this`、别名构造、数组和自定义删除器的边界；建立导航 |
| 06 STL 容器和算法 | 中上 | 常见容器、复杂度、失效规则较全 | 红黑树、哈希 rehash、算法选择、透明比较；移出误放的网络笔记 |
| 07 手写实现题 | 较完整 | 已覆盖容器、并发、定时器和后端组件 | malloc/free、operator new/delete、allocator、deque、map/set、optional、any |
| 08 C++11/14/17 | C++14/17 完整，C++11 较碎 | 新标准归属总体清楚 | 补 `noexcept`、委托构造、继承构造；清理空白和重复笔记 |
| 09 异常安全 | 中等 | 异常等级、RAII、构造析构异常覆盖较好 | 栈展开、异常对象、catch 匹配、`exception_ptr`；建立导航 |
| 10 多线程并发 | 中上 | 锁、条件变量、atomic、CAS 主线齐全 | 内存序实验、ABA、伪共享、无锁结构回收、并发停止协议；建立导航 |
| 11 模板和泛型 | 基础覆盖，示例偏少 | SFINAE、traits、CRTP 已有 | 依赖名、两阶段查找、推导规则、折叠表达式、Concepts；大量增加最小代码 |
| 12 编译链接运行时 | 中等 | 编译流程、符号、动态库和启动已有 | ELF、重定位、PLT/GOT、加载器、ODR、符号可见性；建立调用链导航 |
| 13 Linux 后台 | 中等，广度不足 | IPC、epoll、TCP、mmap 已覆盖 | socket 数据流、粘包、信号、daemon、事件循环、连接生命周期；建立导航 |
| 14 设计模式 | 广度完整 | GoF 模式基本齐全 | 相似模式对比、C++ 所有权、线程安全、反模式和面试口述版 |
| 15 嵌入式 Linux | 入门主线完整 | 启动、设备树、platform、DMA、总线覆盖合理 | GPIO/pinctrl/clock/regulator、PM、锁与屏障、驱动调试闭环 |
| 16 Linux 性能分析 | 工具入门完整，方法论不足 | perf、火焰图和常用统计工具已有 | USE 方法、CPU/cache/NUMA、锁竞争、内存、IO、ftrace/eBPF；增加案例闭环 |
| 17 Linux 调试工具 | 明显不足 | GDB 与远程调试已有 | core dump、sanitizer、Valgrind、strace、readelf/objdump、crash/ftrace/bpftrace |

## 4. 补充顺序

### 第一优先级：先把入口和断裂的知识链补齐

1. 为缺少入口的 11 个主题增加 `README.md`。
2. 修复空白笔记、错误命名和明显放错主题的内容。
3. 更新总览中的 17 个主题、数量和推荐学习顺序。

### 第二优先级：补最影响面试和工程理解的缺口

1. 手写实现：malloc/free、operator new/delete、allocator。
2. 编译运行时：ELF、重定位、PLT/GOT、动态加载器。
3. Linux 后台：从 `recv` 到业务回调的完整数据流。
4. 性能与调试：从症状、证据、假设到验证的闭环。

### 第三优先级：提升已有笔记的可读性

1. 将散落的小片段通过 README 串成学习路径，不删除原文件。
2. 为抽象主题增加“对象/数据在哪里、谁拥有、何时变化”的说明。
3. 为并发和运行时主题增加时间线、状态机和失败路径。
4. 为复杂代码增加不变量、边界、复杂度和教学实现声明。

## 5. 优先制作视频的主题

静态文字最难表达“随时间变化的状态”，因此视频优先选择：

1. 虚函数、多继承与虚继承下的对象布局和调用过程。
2. malloc/free 的空闲链表、分割、合并和碎片。
3. shared_ptr/weak_ptr 控制块与循环引用。
4. 条件变量、虚假唤醒和生产者消费者时间线。
5. ELF、PLT/GOT 与延迟绑定。
6. epoll Reactor 中一次网络事件的数据流。
7. perf 采样、火焰图与热点定位闭环。
8. core dump、寄存器、栈帧到源码行的崩溃还原过程。

视频只承担动态解释，详细代码和命令仍以 Markdown 笔记为准。

## 6. 本轮已经完成

1. 17 个主题均已具备入口 README。
2. 手写实现题由 38 个扩展到 46 个，新增 malloc/free、operator new/delete、allocator、deque、红黑树/map、stack/queue、optional、any。
3. 重写 vector 主实现，补齐原始存储、对象生命周期和异常回滚。
4. 补充 RTTI、enable_shared_from_this、异常栈展开、内存序、模板两阶段查找。
5. 补充 ELF/PLT/GOT、epoll 数据流、设备树资源进入驱动、USE 性能方法、core dump、sanitizer 和调试工具选择。
6. 清除空白 Markdown；原有附件和旧版本笔记均保留。
