在 **构造函数执行期间**，对象还没有完全构造完成，此时：

- 派生类部分还没初始化完
- **虚表指针 (vptr) 还指向当前正在构造的类，而不是最终派生类**

因此即使你在构造函数中调用虚函数：

```
virtual void foo();
Base() { foo(); }  // 调用虚函数
```

如果最终对象是 `Derived`，也**不会调用到 `Derived::foo()`**，而只会调用 `Base::foo()`。