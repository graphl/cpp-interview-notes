**抽象类（Abstract Class）**
 含有纯虚函数，不能直接实例化，只能作为基类。

```
class Shape {
public:
    virtual void draw() = 0; // 纯虚函数
};
```

**特点**：

- 不能直接实例化。
- 可以有 **成员变量**、**普通成员函数**、**构造函数/析构函数**。
- 既可以有 **实现**，也可以留出抽象接口给派生类实现。