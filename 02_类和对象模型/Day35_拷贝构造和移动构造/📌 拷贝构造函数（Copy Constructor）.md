## 📌 拷贝构造函数（Copy Constructor）

- **作用**：用一个已存在的对象来初始化一个新对象，**复制数据**。

- **参数**：通常形式是

  ```
  ClassName(const ClassName& other);
  ```

- **传入的是左值引用**，说明要复制已有对象。

- **特点**：

  - 会分配新资源（深拷贝时），保证新对象和原对象独立。
  - 比如复制一个字符串对象，要开辟新内存并复制数据。

