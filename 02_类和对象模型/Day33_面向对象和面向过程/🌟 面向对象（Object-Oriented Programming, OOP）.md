## 🌟 面向对象（Object-Oriented Programming, OOP）

1. **核心思想**
   - 程序以 **对象（类的实例）** 为核心，数据与操作绑定在一起。
   - 通过封装、继承、多态，增强复用性和可维护性。
2. **特点**
   - **封装**：数据和操作捆绑在对象里，对外隐藏内部实现。
   - **继承**：复用已有类的行为，扩展功能。
   - **多态**：通过基类接口，使用不同子类实现，实现灵活扩展。
   - 更适合大型系统开发。