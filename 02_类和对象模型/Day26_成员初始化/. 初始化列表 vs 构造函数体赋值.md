## **. 初始化列表 vs 构造函数体赋值**

- **初始化列表**：在对象构造的同时直接初始化成员
- **构造函数体赋值**：是先默认初始化（或零初始化），再赋值 → 可能多一次操作

```
struct A {
    int x;
    A(int v) : x(v) {} // ✅ 直接初始化
};

struct B {
    int x;
    B(int v) { x = v; } // ⚠️ 先默认初始化 x，再赋值
};
```

