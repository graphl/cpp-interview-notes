## **多基类 & 虚基类初始化顺序**

- 多基类初始化顺序：按**继承列表的声明顺序**
- 虚基类总是**最先被初始化**，且只会初始化一次（即使多重继承出现多次）

```
struct VB { VB() { cout << "VB\n"; } };
struct A : virtual VB { A() { cout << "A\n"; } };
struct B : virtual VB { B() { cout << "B\n"; } };
struct C : A, B { C() { cout << "C\n"; } };

C obj;
```

